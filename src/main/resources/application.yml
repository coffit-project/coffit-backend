spring:
  application:
    name: coffit-backend

  datasource:
    url: jdbc:mysql://coffit-database.ctkiq6aeimno.ap-northeast-2.rds.amazonaws.com:3306/coffit?serverTimezone=Asia/Seoul
    username: root
    password: coffit1234!
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

server:
  address: 0.0.0.0
  port: 8080
